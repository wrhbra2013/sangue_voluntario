export declare class CachedIteratorIterable<T> implements Iterable<T> {
    private iterator;
    private cached;
    constructor(iterator: Iterator<T>);
    [Symbol.iterator](): Iterator<any>;
    _next(index: number): IteratorResult<T>;
}
